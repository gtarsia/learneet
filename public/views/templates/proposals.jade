h1 Proposed changes
button#addProposal(onclick= onClickAddProposal) Add New
#proposals
  ul
    script#proposals-template(type="x-tmpl-mustache") 
      {{#props}}
      .frame
        input#proposal-id(type="hidden" value="{{proposalId}}")
        img.avatar(src="/images/avatar.png")
        a.description(href="{{url}}") {{description}}
        i  by Him 
        input#acceptBtn(type="image" src="/images/add-icon.png")
        input#rejectBtn(type="image" src="/images/remove-icon.png")
      {{/props}}