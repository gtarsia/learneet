- var authenticated = true;

#parentHeader.parentContainer
  #childHeader.childContainer
    include header_common
    if authenticated
      include header_logged
    else
      include header_not_logged