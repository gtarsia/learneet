.partial.article(style='display:none')
  .main-left
    include templates/score

  .main-center
    .crumb
      a#article-crumb Article
    include templates/viewable-article
    a#editArticle(href= edit_url) Edit
    .rectangular-frame
      h1
        a.dependencies Dependencies
      select.dependency(type="text" placeholder="Type words included in the title of the article you want to depend on...")
      button.add-dependency Add
      .dependency.list
      include templates/dependencies
    .changes.rectangular-frame
      h1.title Proposed changes
      .list
      include templates/article-change-preview-template
    .rectangular-frame
      h1 Information